server {
	listen 80;
	listen [::]:80;
	server_name glyph-motion.slate-app.online;

	location ^~ /.well-known/acme-challenge/ {
		root /var/www/html;
		default_type text/plain;
		try_files $uri =404;
	}

	location / {
		return 404;
	}
}
