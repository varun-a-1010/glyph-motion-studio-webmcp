// The Studio is a pure client-side app: GSAP needs the DOM and WebMCP tools
// register on the live page. No server rendering, one prerendered shell.
export const ssr = false;
export const prerender = true;
